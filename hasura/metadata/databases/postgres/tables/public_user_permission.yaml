table:
  name: user_permission
  schema: public
object_relationships:
  - name: permission
    using:
      manual_configuration:
        column_mapping:
          permission_id: id
        insertion_order: null
        remote_table:
          name: permissions
          schema: public
  - name: user
    using:
      manual_configuration:
        column_mapping:
          user_id: id
        insertion_order: null
        remote_table:
          name: users
          schema: public
