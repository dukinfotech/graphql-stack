table:
  name: roles
  schema: public
array_relationships:
  - name: role_permissions
    using:
      manual_configuration:
        column_mapping:
          id: role_id
        insertion_order: null
        remote_table:
          name: role_permission
          schema: public
  - name: user_roles
    using:
      manual_configuration:
        column_mapping:
          id: role_id
        insertion_order: null
        remote_table:
          name: user_role
          schema: public
